import React from 'react';
import styles from './Transfer.module.css';

const Transfer: React.FC = () => (
  <div className={styles.Transfer} data-testid="Transfer">
    Transfer Component
  </div>
);

export default Transfer;
