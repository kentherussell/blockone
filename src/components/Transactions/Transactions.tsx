import React from 'react';
import styles from './Transactions.module.css';

const Transactions: React.FC = () => (
  <div className={styles.Transactions} data-testid="Transactions">
    Transactions Component
  </div>
);

export default Transactions;
